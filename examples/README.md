# Examples

This directory contains example scripts demonstrating how to use the FSC (Function-to-Structure Coupling) package.

---

## Requirements

Some examples rely on additional packages that are not required for the core FSC library.

Install optional dependencies:

```bash
pip install enigmatoolbox nilearn
